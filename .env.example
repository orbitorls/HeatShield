# ERA5 / CDS API (Copernicus Climate Data Store)
# Register at: https://cds.climate.copernicus.eu — paste key from your profile
CDSAPI_URL=https://cds.climate.copernicus.eu/api
CDSAPI_KEY=

# TMD (Thai Meteorological Department)
# Current API endpoint: https://data.tmd.go.th/api/Weather/v1
# Register at TMD to get your API key
TMD_API_KEY=your_tmd_api_key_here
TMD_BASE_URL=https://data.tmd.go.th/api/Weather/v1

# App
HEATSHIELD_ENV=dev
HEATSHIELD_LOG_LEVEL=INFO

# GPU override — set to 1 to force CPU training for both XGBoost and LightGBM
# (useful on machines without CUDA/OpenCL, or to avoid GPU contention during parallel --workers runs)
HEATSHIELD_FORCE_CPU=0

# LightGBM device override — takes priority over HEATSHIELD_FORCE_CPU
# Valid values: cpu, gpu, cuda  (leave blank to auto-detect)
LGBM_DEVICE=

# XGBoost tuning rounds — controls boosting rounds during Optuna trials
# Lower = faster tuning, higher = better params (default: 50)
XGB_TUNE_ROUNDS=50
