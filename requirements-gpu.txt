# HeatShield AI — GPU-accelerated training dependencies
# ใช้สำหรับการเทรนด้วย GPU เต็มประสิทธิภาพ (Full GPU training)
# Install: pip install -r requirements-gpu.txt

# --- core data / numerics ---------------------------------------------------
numpy>=1.26.0,<3.0.0
pandas>=2.2.0,<3.0.0
pyarrow>=16.0.0
scikit-learn>=1.5.0,<2.0.0
scipy>=1.13.0,<2.0.0
python-dateutil>=2.9.0
pyyaml>=6.0.1

# --- pydantic --------------------------------------------------------------
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.4.0

# --- HTTP clients -----------------------------------------------------------
httpx>=0.27.0
tenacity>=8.2.0

# --- ML backends with GPU support -------------------------------------------
# XGBoost with CUDA support
xgboost>=2.1.0
# LightGBM with GPU support (CUDA/OpenCL)
lightgbm>=4.5.0
# CatBoost with GPU support
catboost>=1.2.7
optuna>=3.6.0
shap>=0.46.0
imbalanced-learn>=0.13
joblib>=1.4

# --- ERA5 / NetCDF stack ---------------------------------------------------
cdsapi>=0.7.0
xarray>=2024.1.0
netCDF4>=1.7.0

# --- visualization ---------------------------------------------------------
matplotlib>=3.8.0
seaborn>=0.13.0

# --- testing ---------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0
